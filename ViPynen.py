import urllib.request
import requests
import json



main():
    while True:
        kommando = input("Språk/Kieli/Language: sv, fi, en")
        #TODO: Next step will be according to language choice
        if kommando = "sv":
            kommando = input("Skriv in kommando eller skriv kommandon \n för att se möjliga alternativ. Skriv sluta för att avlutsa")
            if kommando = "avsluta":
                break
